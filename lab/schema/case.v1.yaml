# NAT Lab Case Schema (v1)
#
# This is a documentation schema used to keep case metadata consistent.
# It is not a strict JSONSchema; the lab runtime may validate a subset.
version: v1
id: core-01
description: NAT1 x NAT1 baseline

side_a:
  nat_label: NAT1            # NAT1|NAT2|NAT3|NAT4
  rfc4787:
    mapping: EIM             # EIM|APDM (ADM reserved)
    filtering: EIF           # EIF|ADF|APDF
  frp:
    nat_type: EasyNAT        # EasyNAT|HardNAT
    behavior: BehaviorNoChange
  port_change: none          # none|regular|irregular

side_b:
  nat_label: NAT1
  rfc4787:
    mapping: EIM
    filtering: EIF
  frp:
    nat_type: EasyNAT
    behavior: BehaviorNoChange
  port_change: none

notes: []
