package poc

const (
	// LocalAPIHost is the required Host header value for all LocalAPI requests.
	LocalAPIHost = "local-miopunch.localapi"

	// LinuxOperatorGroup is the Linux group name used to grant non-root LocalAPI access.
	LinuxOperatorGroup = "miopunch-operators"
)
