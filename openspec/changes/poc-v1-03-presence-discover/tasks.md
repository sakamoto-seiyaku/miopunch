## Done

- Freeze presence topic derivation and the fixed JSON payload field set.
- Freeze retained `online` plus retained LWT `offline` semantics.
- Limit this change to discover input data; GUI rendering and stage transitions stay in `poc-v1-07-gui-wizard`.
