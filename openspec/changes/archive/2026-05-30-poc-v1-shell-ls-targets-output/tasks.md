## 1. Runtime Output

- [x] 1.1 Add per-target facts to `sh ls` success output when target is omitted.
- [x] 1.2 Add per-session facts to `sh ls` success output when a target is specified.
- [x] 1.3 Preserve the existing `lines`, `data`, and report content for `sh ls`.

## 2. Tests

- [x] 2.1 Add runtime coverage for `sh ls <peer>` target enumeration facts/data/report.
- [x] 2.2 Add runtime coverage for `sh ls <peer> <target>` session enumeration facts/data/report.

## 3. Verification

- [x] 3.1 Run focused runtime tests for the touched `sh ls` code.
