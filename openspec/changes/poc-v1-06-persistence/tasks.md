## Done

- Freeze the v1 layout under `device/` and `networks/<network_id>/`, including file responsibilities.
- Freeze atomic write and restrictive permission semantics.
- Limit this change to persist primitives and API shape; caller migration lands with the applied business changes.
