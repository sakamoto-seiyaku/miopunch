## Done

- Freeze the 6-stage wizard state model and the per-stage `summary/reason_code/next action` contract.
- Freeze `UserSummary <= 3 lines`, `Evidence` export, and `reason_code <= 12`.
- Keep GUI as the sole owner of rendering and stage transitions; earlier changes only provide input contracts.
